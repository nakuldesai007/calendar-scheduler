# Render.com Deployment Configuration
# Alternative cloud platform for permanent deployment

services:
  - type: web
    name: ai-schedule-creator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python schedule_creator_app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 8080